          echo whats is in this directory?
          ls -a
          echo
          echo ls Java Installed?
          java --version
          echo
          echo is Git installed?
          git --version
          echo what about web tools?
          mvn --version
          gradle --version
          ant -version
          echo
          echo where is the android SDK root?
          echo $ANDROID_SDK_ROOT
          echo
          echo where is the Selenium jars?
          echo $SELENIUM_JAR_PATH
          echo
          echo what is the workspace location?
          echo $RUNNER_WORKSPACE
          echo
          echo who is runnning te script?
          whoami
          echo how is the disk layout?
          df
          echo what environment variables are available?
          env
